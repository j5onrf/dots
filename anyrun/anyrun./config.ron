Config(
    x: Fraction(0.5), // Or your preferred position
    y: Fraction(0.2),  // Or your preferred position
    // width: Absolute(800), // Optional, adjust if needed
    // height: Absolute(0),  // Optional, adjust if needed
    layer: Overlay,
    close_on_click: false,           // Keep this, good for consistency
    show_results_immediately: true, // Keep this, it's the intended way for initial lists
    plugins: [
        "/etc/anyrun/plugins/libapplications.so"
        // Add full paths to other plugins from /etc/anyrun/plugins/ if you want them
    ],
)
