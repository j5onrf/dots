Config(
  desktop_actions: true, // Or false, your preference
  max_entries: 10,       // Or your preferred number, e.g., 5
  terminal: Some(Terminal(
    command: "alacritty", // Or your terminal
    args: "-e {}",
  )),
  sort_by_usage: true, // This is good for seeing recent/frequent apps
  // preprocess_exec_script: Some("/home/j5/.local/share/anyrun/preprocess_application_command.sh") // Only if you've fixed and tested this
)
