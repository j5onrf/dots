//  by j5onrf, from ML4W base
//  Ghost-Expedition (beta) 2025-06-12

//  Ghost-Expedition :: Theme Layout File
//  This file defines the layout and global settings for the bar.
//  It loads its module definitions from the main library file.

{
    // --- 1. GLOBAL BAR SETTINGS ---
    "layer": "top",
    "position": "left",
    "spacing": 0,
    "margin-top": 9,
    "margin-bottom": 9,
    "margin-left": 10,
    "margin-right": 0,

    // --- 2. LOAD MODULE LIBRARY ---
    // This is the most important line. It loads the master library.
    "include": [
        "~/.config/waybar/ghost-exp.jsonc"
    ],

    // --- 3. MODULE LAYOUT ---
    // The high-level overview of the bar's structure.
    "modules-left": [
        "custom/appmenu",
        "hyprland/window",
        "group/settings"
    ],
    "modules-center": [
        "hyprland/workspaces#rw"
    ],
    "modules-right": [
        "group/hardware",
        "clock",
        "custom/exit"
    ]
}
